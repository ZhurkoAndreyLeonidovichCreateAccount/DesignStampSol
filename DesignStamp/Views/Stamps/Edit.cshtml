@model DataLayer.Entities.Stamp

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Stamp</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Name" />
            <div class="form-group">
                <label asp-for="detailName" class="control-label"></label>
                <select asp-for="detailName" class="form-control" asp-items="ViewBag.detailName"></select>
                <span asp-validation-for="detailName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BushingId" class="control-label"></label>
                <select asp-for="BushingId" class="form-control" asp-items="ViewBag.BushingId"></select>
                <span asp-validation-for="BushingId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ColumnId" class="control-label"></label>
                <select asp-for="ColumnId" class="form-control" asp-items="ViewBag.ColumnId"></select>
                <span asp-validation-for="ColumnId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SpringId" class="control-label"></label>
                <select asp-for="SpringId" class="form-control" asp-items="ViewBag.SpringId"></select>
                <span asp-validation-for="SpringId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
